<% if can? :manage, :all %>
  <%= link_to t("course.create"), new_admin_course_path, class: "btn btn-primary" %>
  <%= link_to t("subject.all_subject"), admin_subjects_path, class: "btn btn-primary" %>
<% end %>
<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <h3>
        <%= t "course.all_course" %>
      </h3>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-12" id="course-index">
      <%= render "index" %>
    </div>
  </div>
</div>
